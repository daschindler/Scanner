package at.davidschindler.scanner.models

/**
 * Describes a pixel and the associated brightness value
 * @param x The x value of the pixel
 * @param y The y value of the pixel
 * @param brightness The brightness value of the pixel
 * */
class Pixel (var x: Int, var y: Int, var brightness: Int) {}
